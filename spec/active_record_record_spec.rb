require 'spec_helper'

describe ActiveActiveRecordRecord do
  it 'has a version number' do
    expect(ActiveActiveRecordRecord::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
